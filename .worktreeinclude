# Файлы, которых нет в git, но без которых копия репозитория не работает.
# Перечисленное здесь попадает в каждое новое рабочее дерево (worktree).

# Локальные настройки бэкенда: строки подключения, пароль роли SQL-консоли,
# ключ модели ассистента. Создаётся из app/backend/.env.example.
app/backend/.env

# Состояние приложения: снимок проекции OperationView и выданные логины
# с паролями. Без него история операций в новом дереве начнётся с пустой.
app/backend/data/
